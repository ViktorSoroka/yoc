const users = require('./users-controller');
const offices = require('./offices-controller');
const publishers = require('./publishers-controller');

module.exports = {
  users,
  offices,
  publishers,
};
